'use strict';

var React = require('react');

var AddContactPage = React.createClass({
    render: function() {
        return (
            <h1>Add Contact</h1>
        );
    }
});

module.exports = AddContactPage;